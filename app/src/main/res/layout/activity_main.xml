<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <EditText
        android:id="@+id/your_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Seu Texto Aqui"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.google.android.material.button.MaterialButton

        android:id="@+id/btn_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@+id/your_text"
        app:layout_constraintEnd_toEndOf="@+id/your_text"
        app:layout_constraintStart_toStartOf="@+id/your_text"
        android:layout_marginBottom="45dp"
        android:text="click"/>

    <com.google.android.material.button.MaterialButton

    android:id="@+id/btn_edit_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintBottom_toTopOf="@+id/your_text"
    app:layout_constraintEnd_toEndOf="@+id/your_text"
    app:layout_constraintStart_toStartOf="@+id/your_text"
    android:layout_marginBottom="90dp"
    android:text="Apagar o texto"/>

</androidx.constraintlayout.widget.ConstraintLayout>